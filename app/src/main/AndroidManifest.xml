<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ht">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HT">
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".EachCountryDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".CountryWiseDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".EachDistrictDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".DistrictWiseDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".EachStateDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".StateWiseDataActivity" android:theme="@style/Theme.AppCompat" />
        <activity android:name=".WorldDataActivity" android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>